using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tileAutoMoveSet : MonoBehaviour
{
    // Start is called before the first frame update

    public int idTile = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
